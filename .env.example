# Copy to .env and fill in. Never commit .env — it holds secrets.

# Signs every member card. The server generates one on first run if this is
# missing. Losing it invalidates every card already printed, so keep a backup.
ENTRY_SECRET=

# Which database to run on. "sqlite" is the default and what the reception
# laptop should stay on: it is the only one that keeps working without
# internet. "mongo" is refused at startup if MB_MONGO_URI is not set — it
# never silently falls back, because that would mean a day of attendance
# written into a local file nobody looks at again.
MB_DB_BACKEND=sqlite

# Where the SQLite file lives, relative to the folder holding the app.
MB_SQLITE_PATH=academy.db

# Only read when MB_DB_BACKEND=mongo.
#
# The mongodb+srv:// form needs a DNS SRV/TXT lookup, which some networks
# filter — a small business's wifi is a plausible place for that. If it fails
# to resolve, use the non-SRV form instead, which names the hosts directly:
#
#   MB_MONGO_URI=mongodb://h1.example:27017,h2.example:27017,h3.example:27017/?replicaSet=atlas-xxxx&tls=true&authSource=admin
#
MB_MONGO_URI=
MB_MONGO_DB=mb_ballet
