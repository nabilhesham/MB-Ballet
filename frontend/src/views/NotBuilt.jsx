import Empty from '../components/Empty';

/**
 * Placeholder for routes not yet ported. Phase 2 only ports the Dashboard;
 * every other view lands in phase 3, one at a time, each compared against
 * its still-live counterpart on / before the next one starts.
 */
export default function NotBuilt() {
  return <Empty title="Not built here yet">This view is still served at the old address.</Empty>;
}
