[pytest]
testpaths = tests
markers =
    sqlite_only: describes the SQLite backend itself (schema introspection),
        not app behaviour — skipped when running against a document store.
    parity: runs both backends side by side and compares. Needs
        MB_TEST_MONGO_URI; skipped without it.
